import test from 'ava'

// check input
// process
// format result

test('empty object returns empty array', t => {
  const expected = true
  const value = true
  t.is(value, expected, 'message');
})
