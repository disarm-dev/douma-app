import test from 'ava'

// steps
// 0. take array of objects of different shapes
// 1. create array of unique fields from all records
// 2. iterate all records to create a row for each, using fields
// 3. return array of rows with header row (using lib?)

// check input
// process
// format result

test('empty object returns empty array', t => {
  const expected = true
  const value = true
  t.is(value, expected, 'message');
})
